一：指针
    指针变量   int  *ptr,**ptr1
    指针数组   int *px[3]
    数组的指针变量  int (*ptr)[3],(*ptr1)[23]
    指针类型   int *    , double * 
    指针获取   &变量  &数组
    指针运算（指针+n  指针-n  ， *指针    指针[下标]）
    指针常量
    指向常量的指针  
    函数的指针
    数组的指针 
    函数之间传递参数   （传数值 ，传指针 ，C++新加 传变量名）
    函数返回参数（返回数值，返回指针，C++新加 返回引用）
二：struct 
    定义结构类型
    struct  MYtype
    {  int  age;
       double high;
       char name[10];
    };
    MYtype  m,n[2],*p,*p1[3];
    m.A=10;
    p=&m;
    p->A=10;
   
三：链表
    链表结构
    链表操作（插入结点，删除结点，修改结点，查询结点，遍历链表，链表排序，链表结点计数）
    链表应用   用你创建的链表保存四位同学的信息
               name    age     high     
               姓名    年龄   身高
               张三     20    1.65
               李四     19     1.78
               王五     21     1.89
               赵三强   20     1.86
   请将四位同学信息保存在链表中，然后对此链表做操作  1：找出身高最高的同学，最低的同学，平均身高
                                                     2：输出身高最高的同学的姓名
                                                    
   提示：先定义结构
         再定义空链表  
         再编写插入结点，删除结点，查询结点的函数
         反复调用插入结点函数将上述四个结点保存在链表中
         对链表做各种查询得出上述操作结果，如找出身高最高同学的姓名等
         用到[动态内存分配 malloc或 calloc 函数,释放内存用free 函数） 函数为每个新结点分配数据，
         如用C++知识 申请动态内存用运算符new ,释放内存用 delete

四：熟练掌握函数的定义，声明（原型），函数的调用的格式和规定，把本次作业全部自己完成（原来是几人合作的
，现在自己独立全部完成）
